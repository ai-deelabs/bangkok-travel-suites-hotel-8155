# Beds24 API V2 — TEST PHASE
# Copy to .env (gitignored) for local dev, and set the same keys in Vercel → Preview.

# ⚠️ TEMPORARY access token (expires ~24h). No refresh logic in this phase —
# regenerate from the Beds24 control panel when it expires.
BEDS24_ACCESS_TOKEN=

# Property + room under test (DeeLabs Hotel → Deluxe Room)
BEDS24_PROPERTY_ID=335864
BEDS24_ROOM_ID=694923
